# Design-change-log archive shards

Dated shards (`YYYY-MM.md`) holding the full rationale of past design changes. The
live summary lives in [`../design-change-log.md`](../design-change-log.md); only open
a shard when you need the reasoning behind a one-liner. One shard per month.
