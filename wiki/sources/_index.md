# Sources — catalog

Every ingested source, one line each: id, title, and what it covers. Loaded on demand;
the root [`../index.md`](../index.md) points here. Updated on every ingest.

For each `src-XXXX`: the LLM-written summary is the single file `sources/src-XXXX.md`;
the immutable original sits flat in [`../raw/`](../raw/) under its own filename (mapped
via that file's `raw:` frontmatter field).

_Count: 0 · Updated: —_

<!-- - src-0001 — "Title" (Author, YYYY) — one line on what it covers. -->
_(none yet)_
