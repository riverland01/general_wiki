# Update-log archive shards

Dated shards (`YYYY-MM.md`) holding the full detail of past content events. The live
summary lives in [`../update-log.md`](../update-log.md); only open a shard when you
need the detail behind a one-liner. One shard per month.
