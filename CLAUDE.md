# Knowledge Wiki — Claude Code entry point

The canonical project instructions live in **[`AGENTS.md`](AGENTS.md)** — read it in
full; everything there applies. This file is a thin pointer so Claude Code has an entry
point, and holds no instructions of its own (single source of truth).

**Skills:** `vault-ingest` and `vault-query` in `.claude/skills/` auto-activate by
their descriptions — no manual invocation needed. They, and `AGENTS.md`, defer to
`design/` as the source of truth.
